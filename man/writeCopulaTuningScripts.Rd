% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/writeCopulaTuningScripts.R
\name{writeCopulaTuningScripts}
\alias{writeCopulaTuningScripts}
\title{Write scripts for copula prior tuning}
\usage{
writeCopulaTuningScripts(
  output_dir = ".",
  n = 18,
  group_sizes = c(6, 4, 4, 4),
  data_gen_hyper_mu = c(1, 0.5, 0.25, 0.2, 0.1, 0.25),
  data_gen_hyper_prec = c(400, 400, 20, 800, 800, 20),
  prior_alpha_cop = c(1, 1, 1, 1),
  prior_hyper_mu = c(1, 0.5, 0.25, 0.2, 0.1, 0.25),
  prior_hyper_prec = c(5, 5, 2, 5, 5, 2),
  n_sim = 500,
  n_cores = max(1, parallel::detectCores() - 1),
  mcmc_niter = 3500,
  mcmc_nburnin = 1000,
  mcmc_nchains = 16,
  output_prefix = "model_select",
  seed = 8001,
  overwrite = FALSE
)
}
\arguments{
\item{output_dir}{Directory where the scripts should be written.}

\item{n}{Total sample size.}

\item{group_sizes}{Numeric vector giving the sample size in each dose group.
Must have length >= 2 (one entry per dose group). Each element should
typically be >= 3 to allow stable estimation of group-specific parameters.
The default \code{c(6, 4, 4, 4)} corresponds to the four-dose example from
COBRA-DOSE paper.}

\item{data_gen_hyper_mu}{Hyper-mean parameters used to generate simulated data.}

\item{data_gen_hyper_prec}{Hyper-precision parameters used to generate simulated data.
These are typically more concentrated (higher precision) than the analysis
priors, ensuring the simulated data has appreciable dependence to allow
meaningful discrimination among copula families.}

\item{prior_alpha_cop}{Dirichlet prior parameters for the four copula model
probabilities (independence, Clayton, Gaussian, Gumbel). Must be a positive
numeric vector of length 4.}

\item{prior_hyper_mu}{Hyper-mean parameters used as priors in the fitted model.}

\item{prior_hyper_prec}{Hyper-precision parameters used as priors in the fitted model.}

\item{n_sim}{Number of simulation repetitions per true copula scenario.}

\item{n_cores}{Number of parallel cores to use in \code{sim_cop.R}.}

\item{mcmc_niter}{Number of MCMC iterations for each simulated dataset.}

\item{mcmc_nburnin}{Number of burn-in iterations for each simulated dataset.}

\item{mcmc_nchains}{Number of MCMC chains. Must be a positive multiple of 4.}

\item{output_prefix}{Prefix used for output CSV files.}

\item{seed}{Random seed base used for data generation. The default
\code{seed = 8001} is offset from the marginal tuning default (\code{seed = 1})
so that simulated datasets in marginal and copula tuning workflows
are disjoint.}

\item{overwrite}{Logical; if \code{FALSE}, existing files will not be overwritten.}
}
\value{
Invisibly returns a list with the paths to the generated scripts.
}
\description{
This function writes two R scripts for copula prior tuning:
\code{cop_fun.R}, which sets up and compiles the NIMBLE model, and
\code{sim_cop.R}, which runs the simulation study over copula scenarios.
}
\details{
The generated \code{cop_fun.R} script fits a Bayesian model in which the
marginal distributions are fixed to gamma for both biomarkers, while
the copula family is treated as a discrete unknown selected via the
latent variable \code{zc} (1 = independence, 2 = Clayton, 3 = Gaussian,
4 = Gumbel). This follows the second simulation study in COBRA-DOSE
paper, in which copula priors are tuned under the assumption of
gamma marginals.

Initial values cover the four \code{zc} starting values (one per candidate
copula); this requires \code{mcmc_nchains} to be a positive multiple of 4.
The default \code{mcmc_nchains = 16} assigns 4 chains per copula starting
value, matching the configuration used in COBRA-DOSE paper. Other
valid values are 4, 8, 12, 20, etc. Smaller values run faster but may
give noisier estimates of the posterior model selection probabilities.
}
\section{When to re-run prior tuning}{

The default \code{prior_alpha_cop = c(1, 1, 1, 1)} was used in COBRA-DOSE
paper for a specific setting: total sample size 18, four dose groups
of sizes c(6, 4, 4, 4), gamma marginals, and the default
\code{prior_hyper_mu} / \code{prior_hyper_prec}. Under that setting, a uniform
Dirichlet prior produced well-calibrated copula model selection (see
Table 3 of COBRA-DOSE paper).

Users applying CobraDose to a substantially different setting ---
different total sample size, different group structure, or different
prior hyperparameters --- should re-run \code{writeCopulaTuningScripts()}
with their own setting and verify that the diagonal of the resulting
model probability matrix is approximately equal across rows and
clearly higher than the off-diagonal entries. If a particular copula
family is systematically under-selected, increase the corresponding
entry of \code{alpha_cop}.

If gamma is not an appropriate marginal family for the user's data,
\code{alpha_cop} may need to be calibrated using a different marginal distribution; please run
\code{writeMarginalTuningScripts()} first to confirm.
}

\examples{
\dontrun{
# Generate the two scripts:
out <- writeCopulaTuningScripts(
  output_dir = tempdir(),
  n = 18,
  group_sizes = c(6, 4, 4, 4),
  overwrite = TRUE
)

# Then source the simulation script:
source(out$sim_cop, chdir = TRUE)

# Inspect a result file:
csv <- read.csv(file.path(tempdir(), "model_select_cop_1.csv"))
summarizeModelProbs(csv[, 2:5])
}

}
